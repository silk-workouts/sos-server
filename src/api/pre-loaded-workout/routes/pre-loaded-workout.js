'use strict';

/**
 * pre-loaded-workout router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::pre-loaded-workout.pre-loaded-workout');
